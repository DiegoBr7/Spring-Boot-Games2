# Nome da aplica��o
spring.application.name=games2

# Configura��es do banco de dados Oracle
spring.datasource.url=jdbc:oracle:thin:@//oracle.fiap.com.br:1521/ORCL
spring.datasource.username=RM553090
spring.datasource.password=270695

# Flyway para controle de vers�o do banco de dados
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true

# Configura��es do JPA e Hibernate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Dialeto do Hibernate para Oracle (opcional, mas recomendado)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.OracleDialect



# Gerenciamento de transa��es (opcional)
spring.jpa.open-in-view=false
